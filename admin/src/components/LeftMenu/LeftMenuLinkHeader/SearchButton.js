import styled from 'styled-components';

const SearchButton = styled.button`
  padding: 0 10px;
  color: #1C2D3D;
  line-height: normal;
  &:focus {
    outline: 0;
    color: #1C2D3D;
  }
`;

export default SearchButton;
